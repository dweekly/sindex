# FTP deployment configuration (copy to .env and fill in values)

# FTP server hostname
FTP_HOST=server298.com

# FTP username
FTP_USER=dew_ftp

# FTP password (keep .env out of git; do NOT commit secrets)
FTP_PASS=REPLACE_WITH_PASSWORD

# Remote directory to sync into
# The expected path is '/www/www-2025' — run verify before deploying
FTP_REMOTE_DIR=/www/www-2025

# Needed to pull YouTube metadata 
YOUTUBE_API_KEY=REPLACE_WITH_API_KEY
