{
  "name": "sindex-website",
  "version": "1.0.0",
  "description": "Sinister Dexter band website with image processing",
  "scripts": {
    "process-images": "node process-images.js",
    "process-images:force": "node process-images.js --force",
    "watch-images": "node process-images.js --watch",
    "convert-heic": "./convert-heic.sh",
    "build:images": "npm run convert-heic && npm run process-images",
    "build:css": "npx tailwindcss -i src/styles.css -o public/styles.css --minify",
    "build:html": "node build.js",
    "sitemap": "node generate-sitemap.js",
    "test": "node test-site-advanced.js",
    "test:quick": "node test-site.js",
    "build": "npm run build:images && npm run build:css && npm run build:html && npm run sitemap",
    "build:test": "npm run build && npm run test",
    "build:force": "npm run convert-heic && npm run process-images:force && npm run build:css && npm run build:html",
    "serve": "cd public && python3 -m http.server 8000 || python -m SimpleHTTPServer 8000",
    "dev": "npm run build && npm run serve",
    "clean": "rm -rf public/images/* .image-cache.json",
    "deploy": "npm run build:css && npm run build:html && wrangler deploy",
    "deploy:staging": "npm run build:css && npm run build:html && wrangler deploy --env staging",
    "preview": "npm run build:css && npm run build:html && wrangler dev",
    "worker:dev": "wrangler dev --local --persist"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.0",
    "chokidar": "^3.6.0",
    "glob": "^10.3.10",
    "handlebars": "^4.7.8",
    "sharp": "^0.33.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "cssnano": "^7.1.0",
    "html-minifier-terser": "^7.2.0",
    "jsdom": "^26.1.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "wrangler": "^4.27.0"
  },
  "author": "",
  "license": "MIT"
}
